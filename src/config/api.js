// API configuration
// Change this to your Render backend URL once deployed
const API_BASE = 'https://greivance-backend.onrender.com';

export const API = {
  USER: `${API_BASE}/user`,
  GRIEVANCE: `${API_BASE}/grievance`,
  ADMIN: `${API_BASE}/admin`,
};

export default API;
